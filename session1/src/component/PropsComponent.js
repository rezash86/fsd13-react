export default function GreetingByName(props) {
  //   console.log(props.name);
  return (
    <p>
      Hello {props.name} {props.prenome}
    </p>
  );
}

// Create a component and name it as StudentCard and
// send your name and age to the component and show the
//  information in the app.js by using your new component
